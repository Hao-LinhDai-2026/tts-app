name: Build APK
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      # Tự động tạo linh kiện thiếu (styles, icon) để lò không báo lỗi
      - name: Create missing resources
        run: |
          mkdir -p app/src/main/res/values
          echo '<resources><style name="AppTheme" parent="android:Theme.Light.NoTitleBar"/></resources>' > app/src/main/res/values/styles.xml
          mkdir -p app/src/main/res/drawable

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Build APK
        run: |
          chmod +x gradlew || true
          gradle assembleDebug --stacktrace

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-final
          path: app/build/outputs/apk/debug/*.apk
